# 决策记录格式

文件位于 `decision-records/`，按 `0001-<slug>.md` 递增编号。

```md
# {已确认的决定}

- 日期：YYYY-MM-DD
- 状态：有效
- 依据：{交互页面、数据来源或讨论}

## 用户证据与决定
{一到三句话，写清选择或排除项。}

## 对后续的影响
{候选范围、排序或待核验事项如何改变。}
```

若决定改变，不删除旧记录；将旧记录状态改为“已被 DR-NNNN 替代”。
